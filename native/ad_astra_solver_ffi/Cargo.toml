[package]
name = "ad_astra_solver_ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ad_astra_solver = { path = "../ad_astra_solver" }
jni = "0.21"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
once_cell = "1"
