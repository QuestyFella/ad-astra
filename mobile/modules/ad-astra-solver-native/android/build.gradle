apply plugin: 'com.android.library'
apply plugin: 'org.jetbrains.kotlin.android'

android {
  namespace "expo.modules.adastrasolver"
  compileSdk 34

  defaultConfig {
    minSdk 23
    targetSdk 34
  }

  sourceSets {
    main.java.srcDirs += 'src/main/java'
  }
}

dependencies {
  implementation project(':expo-modules-core')
}
