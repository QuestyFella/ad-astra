# Data directory

- `raw/` : downloaded catalog files (e.g. Hipparcos, Tycho-2, Gaia subsets).
- `processed/` : preprocessed binaries and offline indexes.

These directories are gitignored; only README and `.gitkeep` are tracked.
